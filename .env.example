# Gradio settings
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=7860
# Set to the public path prefix if behind a proxy (e.g. / )
# GRADIO_ROOT_PATH=

# Model cache settings (optional)
# HF_HOME=./cache/huggingface
